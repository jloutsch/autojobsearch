[pytest]
testpaths = tests
pythonpath = .
markers =
    browser: Browser-level UI tests requiring Playwright (deselect with '-m "not browser"')
    performance: Performance and load benchmarks (deselect with '-m "not performance"')
